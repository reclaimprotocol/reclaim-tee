# TEE_T Environment Configuration

# === Enclave/Platform Settings ===
ENCLAVE_MODE=false
ENCLAVE_DOMAIN=tee-t.reclaimprotocol.org
# For AWS Nitro keep PARENT_CID; for GCP CVM it is unused
PARENT_CID=3
PLATFORM=nitro

# === Port Configuration ===
PORT=8081
HTTP_PORT=8080
HTTPS_PORT=8443

# === Key Management Configuration ===
# Set Google KMS as provider for GCP
KMS_PROVIDER=aws
# Google KMS configuration
GOOGLE_PROJECT_ID=your-gcp-project-id
GOOGLE_KMS_LOCATION=us-central1
GOOGLE_KMS_KEYRING=tee-keys
GOOGLE_KMS_KEY=encryption-key

# === ACME/Let's Encrypt Configuration ===
# Ensure port 80 is reachable on ENCLAVE_DOMAIN for HTTP-01
ACME_DIRECTORY_URL=https://api.buypass.com/acme/directory

# === VSock Proxy Ports (AWS only) ===
# For GCP network mode, these are unused by TEE_T itself but may remain for AWS paths
INTERNET_PORT=8444
KMS_PORT=5000

# === Logging Configuration ===
SERVICE_NAME=tee_t
